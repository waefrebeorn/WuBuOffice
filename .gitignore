# Build trees (configured per-session)
/build/
/build-san/

# Open-font study corpus (fetched by tools/fetch_font_corpus.sh).
# This is a LOCAL artifact (1000+ OFL fonts from the Google Fonts repo), not
# source. The bank studies it via --fontdir; it must never be committed.
/fonts/study-corpus/

# EMNIST Letters benchmark corpus (downloaded by tools/fetch_emnist.sh).
# ~62 MB of IDX files; git-ignored, local artifact only.
/data/emnist/

# stray empty artifact
/model
/build-asan/
/build-dbg/
